// boj 10757 큰 수 A+B
//
#include <iostream>
using namespace std;

// unsigned long long A, B;  // 0 < A,B < 10^10000
string A, B;  // 범위가 크기 때문에 문자열로 받아서 변환함

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    cin >> A >> B;
    
    return 0;
}