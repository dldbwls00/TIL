// boj 11657 타임머신
// bellman-ford
#include <algorithm>
#include <iostream>
using namespace std;

int N, M;  // 도시의 개수, 버스 노선의 개수 (1 <= N <= 500, 1 <= M <= 6000)

bool bellmanford(int s) {
    for (int i = 0; i < N - 1; i++) {
        f
    }
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    cin >> N >> M;
    for (int i = 0; i < M; i++) {
        int A, B, C;  // 버스 노선의 정보 (1 <= A, B <= N, -10,000 <= C <= 10,000)
        cin >> A >> B >> C;
    }
    return 0;
}